
server:
  port: 8085
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://${JDBC_URL:localhost}:${JDBC_PORT:5432}/${DATABASE_NAME:buyandsell}
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:0710}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
  mail:
    port: 587
    host: smtp.gmail.com
    username: testfortest891@gmail.com
    password: iabxmmbebumtizqt
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://${keycloak.domain}/realms/${keycloak.realm}


app:
  name: SandBox task API
  api:
    title: Spring Boot APIs
    version: 1.0.0
    description: Spring Boot APIs description
    base-package: am.devvibes.buyandsell
    contact-name: Norayr Gharibyan
    contact-email: norayrgh@gmail.com
    swagger:
      enable: true


#keycloak:
#  realm: BuyAndSell
#  domain: localhost:8080
#  clientId: buy-and-sell-be
#  clientSecret: TjgE28OqkbR2laqeAI9GezLXkAlVj8cP
#  urls:
#    auth: http://${keycloak.domain}

keycloak:
  realm: buy-and-sell
  domain: localhost:8080
  adminClientId: buy-and-sell-be
  adminClientSecret: TjgE28OqkbR2laqeAI9GezLXkAlVj8cP
  client: buy-and-sell-be
  urls:
    auth: http://${keycloak.domain}



